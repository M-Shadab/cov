sonar.projectKey=M-Shadab_cov
sonar.organization=m-shadab

# This is the name and version displayed in the SonarCloud UI.
#sonar.projectName=cov
#sonar.projectVersion=1.0


# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
#sonar.sources=.

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8


# old stackoverflow code
# # Source
# sonar.sources=src
# # Where to find tests file, also src
# sonar.tests=src
# # But we get specific here
# # We don't need to exclude it in sonar.sources because it is automatically taken care of
# sonar.test.inclusions=src/**/*.spec.js,src/**/*.spec.jsx,src/**/*.test.js,src/**/*.test.jsx

# # Now specify path of lcov and testlog
# sonar.javascript.lcov.reportPaths=coverage/lcov.info
# sonar.testExecutionReportPaths=coverage/clover.xmla